// Arquivo 1: beneficio_01.drl
package com.example.rules;

import com.motor.decision.dto.PersonDTO;
import com.motor.decision.dto.BenefitDTO;

rule "Benefício para Idosos"
when
    $p : PersonDTO(age >= 65)
then
    BenefitDTO $b = new BenefitDTO();  
    $b.setBenefit("Aposentadoria Especial");
    $b.setRule("beneficio_01.drl");
    $b.setHasBenefit(true);
    $p.addLisBenefit($b);  
end

rule "Sem Benefício para Idosos"
when
    $p : PersonDTO(age < 65)
then
    BenefitDTO $b = new BenefitDTO();  
    $b.setBenefit("Aposentadoria Especial");
    $b.setRule("beneficio_01.drl");
    $b.setHasBenefit(false);
    $p.addLisBenefitNot($b);  
end